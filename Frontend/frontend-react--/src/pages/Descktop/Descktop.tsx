import { useNavigate } from "react-router-dom";
const Descktop =() => {


const navigate = useNavigate()
     navigate('/login')
return (
    <div className=''>
   
    </div>
);
}

export default Descktop;
